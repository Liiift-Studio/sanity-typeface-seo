/** SEO/social field extended with marketplace links — used by Darden Studio (Adobe Fonts, Font Stand) */
import { createSeoField } from './createSeoField'

export const seoFieldWithLinks = createSeoField({ marketplaceLinks: true })
